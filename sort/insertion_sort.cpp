#include <bits/stdc++.h>

void trace(int array[], int n)
{
  for (int i = 0; i < n; i++)
  {
    if (i > 0)
    {
      std::cout << " " << std::endl;
    }
    std::cout << array[i] << std::endl;
  }
  std::cout << std::endl;
}

int main()
{
}